public class Junk {

    // public boolean sendMessage(Message[] Message){
//     for(int k = 0;k<Message.length; k++){
//         if (Message[k].equals(null)){
//             return true;
//         }
//     }
//     return false;
// }

// public boolean sendMessage(Message msg){
//     if (messageCount < inbox.length){
//         User reciever = msg.getReceiver();
//         inbox
//         // boolean messageSent = receiver.addMessage(msg);
//     }
//     return true;
    // User receiver = msg.getReceiver();
    // boolean messageSent = receiver.addMessage(msg);
    // return messageSent;

    // public String addMessage(Message msg){
//     if (messageCount == inbox.length){
//     return "Sorry, the inbox is full!";
    
//         //return "Message is added!";
//     }
    
//     } 
    
//     {
    
//     }}

// inbox[messageCount] = msg;
//     messageCount++;


 // if (messageCount == inbox.length){
    //     return "Message is added!";
    // }
    // //Message.add(Message msg);

    ___________________________________________

    // public String readMessage(){
//     if(messageCount == 0){
//         return "There is no message to read!";
//     }else{
//         return " " + text + " ";
//     }
// }

 // Retrieve the text field of the first message in the inbox
        // Delete the first message and shift remaining messages to the left
        // Decrease the value of messageCount


    
}
